# QCchem v0.1 Credibility Hardening Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Harden QCchem v0.1-alpha so result energies, exact baselines, artifacts, and reports are numerically interpretable and benchmarkable.

**Architecture:** Keep the current package boundaries, but upgrade the result model so raw solver energies, constant shifts, corrected electronic energies, and total energies are explicitly separated. Add an exact-baseline benchmark layer on top of existing solver/backends, and make reports/artifacts consume those structured outputs consistently.

**Tech Stack:** Python, Qiskit, Qiskit Nature, PySCF, NumPy, SciPy, PyYAML, pytest

---

### Task 1: Red tests for energy schema and benchmark outputs

**Files:**
- Create: `tests/integration/test_benchmarks.py`
- Modify: `tests/integration/test_h2_workflow.py`

- [ ] **Step 1: Write failing tests for the new energy fields, exact baseline comparison, and report regeneration**
- [ ] **Step 2: Run `python -m pytest tests/integration/test_h2_workflow.py tests/integration/test_benchmarks.py -v`**
- [ ] **Step 3: Verify failure is due to missing fields/artifacts instead of typos**

### Task 2: Result model and workflow energy normalization

**Files:**
- Modify: `qcchem/core/results.py`
- Modify: `qcchem/core/__init__.py`
- Modify: `qcchem/chem/problem_builder.py`
- Modify: `qcchem/workflow/runner.py`
- Modify: `qcchem/reporting/markdown.py`
- Modify: `qcchem/reporting/jsonio.py`

- [ ] **Step 1: Add explicit raw-vs-corrected energy fields and benchmark summary dataclasses**
- [ ] **Step 2: Normalize Hamiltonian constants so `total_energy = electronic_energy + nuclear_repulsion_energy`**
- [ ] **Step 3: Persist schema version, timestamp, git commit, dependency versions, and wall time**
- [ ] **Step 4: Re-run targeted tests and confirm the energy schema passes**

### Task 3: Exact baseline and mapper compatibility

**Files:**
- Modify: `qcchem/mapping/mapper.py`
- Modify: `qcchem/solvers/base.py`
- Modify: `qcchem/solvers/exact.py`
- Modify: `qcchem/solvers/vqe.py`
- Modify: `qcchem/solvers/__init__.py`

- [ ] **Step 1: Add a NumPy compatibility shim so Bravyi-Kitaev mapping works in the current environment**
- [ ] **Step 2: Save exact baseline results into dedicated artifact files**
- [ ] **Step 3: Compute absolute/relative error and threshold pass/fail automatically**
- [ ] **Step 4: Re-run mapper/benchmark tests and confirm JW/BK consistency**

### Task 4: Config, CLI, and artifact proof runs

**Files:**
- Modify: `qcchem/core/specs.py`
- Modify: `qcchem/io/config.py`
- Modify: `qcchem/cli/main.py`
- Modify: `configs/h2.yaml`
- Modify: `configs/lih.yaml`
- Modify: `configs/h2o_active_space.yaml`

- [ ] **Step 1: Add optional benchmark thresholds/config support**
- [ ] **Step 2: Run `qcchem run -c configs/h2.yaml`, `qcchem run -c configs/lih.yaml`, and `qcchem run -c configs/h2o_active_space.yaml`**
- [ ] **Step 3: Verify each artifact directory contains `result.json`, `report.md`, `resolved_config.yaml`, `run.log`, and exact baseline output**

### Task 5: Documentation and final verification

**Files:**
- Modify: `README.md`
- Modify: `docs/architecture.md`
- Modify: `docs/handoff.md`

- [ ] **Step 1: Document the new energy semantics and credibility boundaries**
- [ ] **Step 2: Run `python -m pytest -v`**
- [ ] **Step 3: Manually inspect generated artifacts for H2, LiH, and H2O(active-space)**

Plan complete and saved to `docs/superpowers/plans/2026-04-10-qcchem-v0.1-credibility-hardening.md`. The user explicitly requested direct execution, so implementation will continue inline in this session.
