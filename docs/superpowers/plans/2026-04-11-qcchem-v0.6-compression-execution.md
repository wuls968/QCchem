# QCchem Compression-Aware Execution Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Upgrade QCchem from compression audit to compression-aware execution with real compressed solve paths, compressed benchmarks, chained active-space+compression+NEVPT2 workflow, and stronger provenance/export artifacts.

**Architecture:** Keep `RunResult` as the atomic artifact and make compression execution an opt-in branch inside the existing `run_spec` workflow. Build compressed fermionic operators in `qcchem/chem/compression.py`, map them through the existing mapper, compare them against uncompressed references via `BenchmarkSummary`, and export provenance through the artifact layer without changing study/benchmark/scan fundamentals.

**Tech Stack:** Python, Qiskit, Qiskit Nature, primitives V2, PySCF, pytest, JSON, optional HDF5 via h5py

---
